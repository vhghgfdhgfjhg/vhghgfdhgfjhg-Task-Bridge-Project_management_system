import React from 'react'

const Datatransfer = ({props}) => {
  return (
    <div>
      {props.map((data)=>(
        (
           <div></div>
        )
      )}
    </div>
  )
}

export default Datatransfer
